/*
function validate(): never {
  throw new Error('테스트 예외')
}

interface Person {
  name: string
  age: number
  address: string
}

type PersonKey = keyof Person // 'name' | 'age' | 'address'

const key: PersonKey = 'name'
const key2: PersonKey = 'age'

const person = {
  name: '이이름',
  age: 40,
  zipcode: '01111',
}

for (const key in person) {
  //const k1: 'name' | 'age' | 'address' = key
}
*/
